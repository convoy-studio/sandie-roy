define [], () ->

    "use strict"
    
    class RendererService

        constructor: () ->

        init: =>
            @resize()
            return

        resize: =>
            return

    return RendererService
